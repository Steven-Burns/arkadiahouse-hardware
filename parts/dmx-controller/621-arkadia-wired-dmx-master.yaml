substitutions:
  device_hostname: "621-esphome"
  device_comment: "Arkadia House DMX controller on board 621."

<<: !include 
  file: ./arkadia-wired-dmx-base.yaml
  vars:
    device_hostname: ${device_hostname}
    device_comment: ${device_comment}

# uncomment this line to create single channel on/off lights for each DMX channel of both universes, which can be useful for testing and debugging.
#<<: !include ./arkadia-dmx-debug-lights.yaml 

#uncomment this line to create RGBWW light entities.
<<: !include ./arkadia-dmx-rgbww-lights.yaml